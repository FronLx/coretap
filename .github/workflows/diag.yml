name: Diag Railway

on:
  workflow_dispatch:

jobs:
  check:
    runs-on: ubuntu-latest
    steps:
      - run: curl -sS -m 30 -o /dev/null -w "HTTP:%{http_code} connect:%{time_connect} total:%{time_total}\n" https://coretap-production.up.railway.app/api/health || echo "CURL FAILED"